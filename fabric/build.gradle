//file:noinspection GrDeprecatedAPIUsage
import groovy.json.JsonOutput
import groovy.json.JsonSlurper
import net.darkhax.curseforgegradle.TaskPublishCurseForge

plugins {
    id 'multiloader-loader'
    alias libs.plugins.fabricLoom
    alias libs.plugins.curseForgeGradle
    alias libs.plugins.modrinthMinotaur
}

apply from: rootProject.file('repositories.gradle')
dependencies {
    minecraft "net.minecraft:minecraft:${minecraft_version}"
    mappings loom.layered() {
        officialMojangMappings()
        parchment("org.parchmentmc.data:${parchment_version}:${parchment_mapping_version}")
    }
    modImplementation("net.fabricmc:fabric-loader:${fabric_loader_version}")
    modImplementation("net.fabricmc.fabric-api:fabric-api:${fabric_api_version}")
    modImplementation("net.blay09.mods:balm-fabric:${balm_version}") {
        changing = balm_version.contains("SNAPSHOT")
    }
    compileOnly("cc.thonly:keine-common:1.21.11-1.0.0")
    modImplementation include("cc.thonly:keine-fabric:1.21.11-1.0.0")
    modImplementation include("software.bernie.geckolib:geckolib-fabric-${minecraft_version}:${geckolib_version}")
    modImplementation include("me.lucko:fabric-permissions-api:${fabric_permissions_api_version}")
    modImplementation include("xyz.nucleoid:server-translations-api:${server_translations_api_version}")
    modImplementation("com.terraformersmc:modmenu:${modmenu_version}")
    modImplementation include("me.shedaniel.cloth:cloth-config-fabric:${cloth_config_version}") {
        exclude(group: "net.fabricmc.fabric-api")
    }
    modImplementation include("eu.pb4:map-canvas-api:${mapcanvas_version}")
    modImplementation include("eu.pb4:placeholder-api:${placeholder_api_version}")

    // Polymer
    modImplementation("eu.pb4:polymer-autohost:[${polymer_version}]")
    modImplementation("eu.pb4:polymer-blocks:[${polymer_version}]")
    modImplementation("eu.pb4:polymer-common:[${polymer_version}]")
    modImplementation("eu.pb4:polymer-core:[${polymer_version}]")
    modImplementation("eu.pb4:polymer-networking:[${polymer_version}]")
    modImplementation("eu.pb4:polymer-reg-sync-manipulator:[${polymer_version}]")
    modImplementation("eu.pb4:polymer-resource-pack-extras:[${polymer_version}]")
    modImplementation("eu.pb4:polymer-resource-pack:[${polymer_version}]")
    modImplementation("eu.pb4:polymer-sound-patcher:[${polymer_version}]")
    modImplementation("eu.pb4:polymer-virtual-entity:[${polymer_version}]")
    modImplementation("eu.pb4:factorytools:[${factorytools_version}]")
    modImplementation("de.tomalbrc:blockbench-import-library:${bil_version}")
    modImplementation("eu.pb4:booklet:${booklet_version}")
    modImplementation include("eu.pb4:sgui:[${sgui_version}]")
    modImplementation files('libs/camera-obscura-1.2.2+1.21.11.jar')

    modCompileOnly("mezz.jei:jei-${minecraft_version}-fabric-api:${jei_version}")
//    modRuntimeOnly("mezz.jei:jei-${minecraft_version}-fabric:${jei_version}")
    modCompileOnly("me.shedaniel:RoughlyEnoughItems-fabric:${rei_version}")
    modRuntimeOnly("me.shedaniel:RoughlyEnoughItems-fabric:${rei_version}")

    modCompileOnly files('libs/create-fly-1.21.11-6.0.9-1.jar')
    modCompileOnly files('libs/polyfactory-0.10.4+1.21.11.jar')
    modCompileOnly files('libs/eiv-fabric-5.2.0+1.21.11.jar')
    modCompileOnly files('libs/polydex2eiv-1.0.9.jar')
    modCompileOnly files('libs/borukva-food-0.1.14+1.21.7.jar')
    modCompileOnly files('libs/borukva-food-exotic-3.10.0+1.21.7.jar')
    modCompileOnly files('libs/borukva-fish-0.1.9+1.21.8.jar')
    modCompileOnly files('libs/FarmersDelight-1.21.7-3.2.4+refabricated.jar')
    modCompileOnly files('libs/farmers-delight-polymer-patch-3.2.3.0+1.21.6.jar')
    modCompileOnly files('libs/moredelight-25.07.28a-1.21.7-fabric.jar')
    modCompileOnly files('libs/moredelight-patch-25.07.28a-1.21.7.jar')
    modCompileOnly files('libs/spanishdelight-1.21.8-1.0.6.jar')
    modCompileOnly files('libs/spanishdelight-patch-1.21.8-1.0.6.jar')
    modCompileOnly files('libs/oceansdelight-port-0.1.0+1.21.6.jar')
    modCompileOnly files('libs/Fishing-101-1.7.1_experimental.jar')
    modCompileOnly files('libs/go-fish-1.8.0+1.21.6.jar')
    modCompileOnly files('libs/brewery-0.12.0+1.21.6.jar')
    modCompileOnly files('libs/polyfactory-0.10.4+1.21.11.jar')
    modCompileOnly files('libs/appleskin-fabric-mc1.21.11-3.0.8.jar')
    modCompileOnly 'maven.modrinth:polydex:1.8.1+1.21.11'

    implementation include('com.squareup.okhttp3:okhttp:4.12.0')
    implementation include('com.squareup.okio:okio:3.4.0')
    //noinspection DifferentStdlibGradleVersion
    implementation include('org.jetbrains.kotlin:kotlin-stdlib:1.9.10')
    implementation include('org.jetbrains.kotlin:kotlin-stdlib-common:1.9.10')
    implementation include('org.jetbrains.kotlin:kotlin-stdlib-jdk8:1.9.10')
    implementation include('org.reflections:reflections:0.10.2')
}

tasks.withType(JavaCompile).configureEach {
    it.options.encoding = "UTF-8"
    it.options.release = 21
    options.compilerArgs << "-Xlint:-deprecation"
    options.compilerArgs += ["-Xlint:none"]
}

loom {
    def aw = project(":common").file("src/main/resources/${mod_id}.accesswidener")
    if (aw.exists()) {
        accessWidenerPath.set(aw)
    }

    mixin {
        defaultRefmapName.set("${mod_id}.refmap.json")
    }

    runs {
        client {
            client()
            setConfigName("fabric Client")
            ideConfigGenerated(true)
            runDir("runs")
        }
        server {
            server()
            setConfigName("fabric Server")
            ideConfigGenerated(true)
            runDir("runs")
        }
        data {
            inherit client
            setConfigName("fabric Data")
            ideConfigGenerated(true)
            runDir("build/datagen")

            vmArg "-Dfabric-api.datagen"
            vmArg "-Dfabric-api.datagen.output-dir=${project(":common").file("src/generated/resources")}"
            vmArg "-Dfabric-api.datagen.modid=${mod_id}"
        }
    }
}

def includeDependencyJar = { File targetJar, List<String> list ->

    def tempDir = new File(buildDir, "tmp/include_jars")
    tempDir.deleteDir()
    tempDir.mkdirs()

    // 解压目标 mod jar
    copy {
        from zipTree(targetJar)
        into tempDir
    }

    // 创建 META-INF/jars 目录
    def metaDir = new File(tempDir, "META-INF/jars")
    metaDir.mkdirs()

    // 复制依赖 jar
    list.each { fil ->
        copy {
            from file(fil)
            into metaDir
        }
    }

    // 修改 fabric.mod.json
    def jsonFile = new File(tempDir, "fabric.mod.json")
    if (jsonFile.exists()) {
        def json = new JsonSlurper().parseText(jsonFile.text)
        if (!json.containsKey("jars")) {
            json.jars = []
        }

        list.each { fil ->
            def jarName = "META-INF/jars/${new File(fil).name}"
            // 避免重复写入
            if (!json.jars.any { it.file == jarName }) {
                json.jars << [file: jarName]
            }
        }

        jsonFile.text = JsonOutput.prettyPrint(JsonOutput.toJson(json))
    } else {
        throw new GradleException("fabric.mod.json not found in ${targetJar}")
    }

    // 重新打包 mod jar
    ant.jar(destfile: targetJar) {
        fileset(dir: tempDir, includes: "**/*")
    }

    tempDir.deleteDir()
}

tasks.remapJar {
    doLast {
        def jarFile = archiveFile.get().asFile
        // 这里传入你的依赖 jar 列表
        includeDependencyJar(jarFile, [
        ])
    }
}
